apiVersion: apps/v1
kind: Deployment
metadata:
  name: doctor-service
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: doctor-service
  template:
    metadata:
      labels:
        app: doctor-service
    spec:
      containers:
        - name: doctor-service
          image: your-dockerhub-username/ms1:latest
          ports:
            - containerPort: 8080
# TODO
# add docker image
